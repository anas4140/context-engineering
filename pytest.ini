[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: tests that make real Anthropic API calls (deselect with -m "not integration")
    slow: tests that take longer than 5 seconds
addopts = -m "not integration"
