# ─────────────────────────────────────────────
#  Context Engineering for AI — Dependencies
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────

# LLM provider
anthropic>=0.40,<1.0

# Embeddings (converts text → vectors)
sentence-transformers>=2.7.0

# Vector store (local, no server needed)
chromadb>=0.5.0

# Utilities
python-dotenv>=1.0.0     # loads .env API keys
tiktoken>=0.7.0          # token counting
rich>=13.7.0             # pretty terminal output
tenacity>=8.3.0          # retry logic for API calls

# Evaluation (Module 6)
ragas>=0.1.0,<0.2.0

# Hybrid search — sparse BM25 retrieval (Module 3)
rank-bm25>=0.2.2

# MCP — Model Context Protocol client/server (Module 10)
mcp>=1.0.0

# Real web search (optional) — set TAVILY_API_KEY in .env to enable
# tavily-python>=0.3.0

# Notebooks (optional)
jupyter>=1.0.0
ipykernel>=6.29.0
